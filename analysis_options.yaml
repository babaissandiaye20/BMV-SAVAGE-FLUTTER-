include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Active quelques règles utiles (tu peux les ajuster selon ton style de code)
    prefer_const_constructors: true
    avoid_print: false  # Autorise les print() en debug
    prefer_final_locals: true
